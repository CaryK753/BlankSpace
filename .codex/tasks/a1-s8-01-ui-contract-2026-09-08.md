# A1-S8-01 UI 工具链与双 Shell 合同冻结

日期：2026-09-08

## 目标

在不安装 UI package、下载浏览器或实现组件的前提下，冻结 S8 的 exact React/router/primitives/Playwright/a11y 候选、双 Shell corpus、固定环境、阈值、诊断与供应链边界。

## TODO

- [x] 阅读 A1-S8-01、S8、RFC-0013、UI templates、测试、安全与供应链边界。
- [x] 核对现有品牌资源与 proposed Route/Navigation/Shell Contract。
- [x] 查询候选 package、browser revision、license、integrity、provenance、scripts 与临时解析树。
- [x] 冻结双 Shell、标准状态、响应式、导航、焦点、overlay、视觉与 accessibility 矩阵。
- [x] 记录候选依赖、浏览器 artifact、安装策略、替代方案、回滚与复审条件。
- [x] 更新 work-item DAG、project status、决策台账和 Agent 入口。
- [x] 运行 build、typecheck、完整 test 与 verify:docs。
- [x] 提交、同步远端并核对 GitHub Actions。

## 本地证据

- 环境：macOS arm64、Node.js v24.20.0、pnpm 10.32.1；供应链临时解析使用 npm 10.9.8。
- Matrix frozen：两个 Shell、八种状态、三个 viewport/browsers、边界两侧响应式、100 pixel visual 容差、零 axe violation 和 stable diagnostics。
- `pnpm build`、`pnpm typecheck`、147 Vitest、8 S1、4 S2、7 S3、4 S4、15 S5、10 S6、5 S7 tests 与 `pnpm verify:docs` 通过。
- 未修改 package/lock，未下载/运行 Playwright browser，未实现组件或生成 baseline。

## GitHub evidence

- 候选 commit：`6fb33f63194a538ee5680e66a47feb09fc80b12a`；PR：`#2`。
- S2 run `34212726416`、S3 `34212726430`、S4 `34212726439`、S5 `34212726528`、S6 `34212726611`、S7 `34212726507` 全部完成并成功。
- 逐 job 核对 Ubuntu 24.04、macOS 15、Windows 2025：S2～S6 各 3 个 jobs，S7 Ubuntu 1 个 job，共 16/16 成功；S7 日志为 5 tests / 5 pass / 0 fail，资源缺失断言步骤成功。

## 边界

- 本项只写决策和供应链文档；不修改 `package.json`/lock，不运行 Playwright，不生成截图。
- React 类型不得进入非 UI Foundation package；业务只消费公开、可序列化 UI Contract。
- S8 Matrix frozen 只表示输入和门槛已固定，不表示任何 Shell、浏览器或 accessibility 行为已经通过。

## 调研快照

- exact candidates：React/React DOM `19.2.8`、React Router `8.3.1`、React Aria Components `1.21.1`、Playwright `1.63.0`、axe `4.13.0`、Inter variable `5.3.0`。
- Playwright browser registry：Chromium/Headless Shell revision `1243`（`153.0.8010.12`）、Firefox `1543`（`155.0`）、WebKit `2359`（`26.6`）。
- npm 10.9.8 临时 `package-lock-only --ignore-scripts --omit=optional` 解析 27 个 dependency nodes；lock metadata 无 install hook，`npm audit` 报告 0 个已知漏洞。`@axe-core/playwright` 发布 manifest 含 `prepare`，因此未来 pnpm 安装仍必须禁用 scripts 并复核 lock/build allowlist。
