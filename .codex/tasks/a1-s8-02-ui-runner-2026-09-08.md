# A1-S8-02 本地双 Shell UI conformance runner

日期：2026-09-08

## 目标

按冻结的 S8 contract 安装 exact UI/test dependencies，实现消费同一 semantic contributions 的 `default-shell` 与 `workbench-shell`，并在本地真实浏览器中验证行为、键盘、焦点、accessibility 与 Chromium visual baseline。

## TODO

- [x] 核对 A1-S8-02、S8 frozen matrix、供应链审查、proposed UI Contract 与当前仓库基线。
- [x] 复核 exact registry identity，安装依赖并对账 pnpm lock/scripts。
- [x] 建立 serializable contribution corpus、稳定 diagnostics 与 transcript schema。
- [x] 实现两个结构不同且消费同一 contribution set 的 React Shell fixture。
- [x] 实现本地 fixture server、三浏览器 behavior/a11y、Chromium visual runner 与清理边界。
- [x] 冻结本地 transcript/baseline，并运行 build、typecheck、完整 test、test:s8、verify:docs。
- [x] 更新 S8/status/work-item/Agent 文档，提交、同步远端并核对 Actions。

## 本地证据

- exact candidates 通过 `pnpm add --save-exact --ignore-scripts` 安装；browser binary 由独立 Playwright install 步骤下载。
- Chromium 1243、Firefox 1543、WebKit 2359 共通过 15 个行为检查与 144 次 axe scan；20 个 Chromium local visual baseline 通过。
- matrix hash：`73a2e570c99f2ca088dcb4b6a2793c58477098558670dae287adb50f639df038`。
- `pnpm build`、`pnpm typecheck`、147 个 Vitest tests、S1～S8 完整 corpus 与 `pnpm verify:docs` 通过。
- PR #3 候选提交 `1486fb0` 的 S2 run `34245446283`、S3 `34245446294`、S4 `34245446182`、S5 `34245446271`、S6 `34245446261` 与 S7 `34245446308` 共 16 个实际 jobs 全部通过。

## 边界

- 只使用 A1-S8-01 已审查的 exact packages 与当前 exact Vite；禁止 install scripts 隐式下载浏览器。
- fixture 只监听 `127.0.0.1`，默认阻断外网，不使用真实账号、业务 API、cookie、credential 或用户数据。
- 这是可删除的 conformance spike，不建立 production UI package、Identity、live API、Native renderer 或部署。
- 本地证据不把 S8 推进为 Pass；Ubuntu canonical visual 与最终 S8 决策由后续工作项负责。
